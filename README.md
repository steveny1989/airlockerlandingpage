# AirLocker Landing Site

Static marketing site for **AirLocker** (pure HTML/CSS/JS).

## Local preview

```bash
python3 -m http.server 8080 --directory .
```

Then open `http://localhost:8080`.

## Deploy (GitHub Pages)

This repo is meant to be hosted via **GitHub Pages** from the `main` branch root (`/`).

